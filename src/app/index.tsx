import { StateView } from '@/components/ui';

export default function SessionsScreen() {
  return (
    <StateView
      icon="film-outline"
      title="No sessions yet"
      message="Sessions arrive in the next milestone."
    />
  );
}
