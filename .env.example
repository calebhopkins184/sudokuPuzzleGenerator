# Base URL of the Mat Review server that proxies YouTube search.
# This is NOT a secret. The YouTube Data API key lives only on that server;
# never put API keys in EXPO_PUBLIC_* variables (they ship inside the app bundle).
EXPO_PUBLIC_MAT_REVIEW_API_URL=
